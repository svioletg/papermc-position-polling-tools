Functions which require FFmpeg
==============================

.. _FFmpeg: https://ffmpeg.org/download.html

.. currentmodule:: positionpolling

Below is a list of functions that are marked as requiring `FFmpeg`_ to be
installed or otherwise available on your PATH (putting the binary in the
current directory is sufficient). If these functions are called and FFmpeg
could not be found, :class:`FileNotFoundError` is raised.

----

.. The "function-list-here" comment below will be dynamically replaced with all
.. functions that have "|requires-ffmpeg|" in their docstrings.

.. function-list-here
